# Binaries and executable files
/palworld-manager
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, generated by `go test -coverprofile`
*.out

# Dependency directories (only if vendor is used, but go mod tidy is preferred)
# vendor/

# Local Palworld data volume (to prevent committing game saves/data to GitHub)
/palworld-data/

# IDEs and Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db
ehthumbs.db

# Local environment configuration or temporary credentials
.env*
